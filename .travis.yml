language: node_js

node_js:
  - "12"
  - "11"
  - "10"
  - "8"

branches:
  only:
    - master

before_script:
  - npm run lint
script:
  - npm run build

jobs:
  include:
    - stage: release
      node_js: lts/*
      script:
        - npm run semantic-release

notifications:
  email:
    on_success: never
    on_failure: always
